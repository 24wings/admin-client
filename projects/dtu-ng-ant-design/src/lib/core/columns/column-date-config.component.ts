import { BasicColumn } from '../basic-column';
import { BasicDynamicComponentConfig } from '../basic-dynamic-component.config';

export class ColumnDateConfig extends BasicColumn {
  format?: string;
}
